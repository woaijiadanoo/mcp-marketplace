---
description: 使用 mcp-feedback-lite 实现人机交互反馈循环，最大化单次对话效果
alwaysApply: true
---

# MCP Interactive Feedback Rules

## 核心原则

每次任务执行完成后，必须向用户征求反馈，而不是直接结束对话。优先使用 `interactive_feedback`，若不可用则降级为 `AskQuestion`。

## 反馈工具优先级

1. **首选** — `mcp-feedback-lite` 的 `interactive_feedback` 工具
2. **降级** — 内置 `AskQuestion` 工具（当 `interactive_feedback` 调用失败、超时、MCP 未连接或返回异常时自动切换）

一旦某次调用 `interactive_feedback` 失败，后续本轮对话中直接使用 `AskQuestion`，不再重试 `interactive_feedback`。

## 工作流程

1. 接收用户指令，执行所需操作
2. 操作完成后，通过反馈工具向用户展示结果摘要并征求下一步指示
3. 根据用户反馈继续执行、调整方案或修复问题
4. 重复 1-3，直到触发退出条件

## interactive_feedback 调用规范

- `project_directory`: 当前工作目录
- `summary`: 简要说明本轮做了什么、当前状态、等待用户什么输入

## AskQuestion 降级调用规范

当降级使用 `AskQuestion` 时，构造一个问题：
- `prompt`: 包含本轮执行摘要 + "请输入下一步指示，或选择结束"
- `options`: 至少包含"继续（我会输入新指令）"和"结束对话"两个选项

示例：
```
AskQuestion({
  questions: [{
    id: "next_step",
    prompt: "【执行摘要】已完成 XXX。\n请选择下一步操作：",
    options: [
      { id: "continue", label: "继续 — 我有新的指令" },
      { id: "done", label: "结束 — 本轮对话到此为止" }
    ]
  }]
})
```

若用户选择"继续"，等待用户在后续消息中输入具体指令后再执行。

## 退出条件

当用户反馈内容满足以下任一条件时，正常结束对话：
- 空白内容（空字符串）
- 包含关键词：退出、结束、完成、exit、quit、done
- 在 AskQuestion 中选择了"结束"选项
